apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginx-redirect-banner.fullname" . }}
  labels:
    {{- include "nginx-redirect-banner.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nginx-redirect-banner.selectorLabels" . | nindent 4 }}
