apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nginx-redirect-banner.fullname" . }}-test-connection"
  labels:
    {{- include "nginx-redirect-banner.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nginx-redirect-banner.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
